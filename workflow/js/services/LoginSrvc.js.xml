<?xml version="1.0" encoding="UTF-8"?>
<Export generator="Cache" version="25">
<CSP name="js/services/LoginSrvc.js" application="/workflow/"><![CDATA[
'use strict';

// login service
function LoginSrvc( RESTSrvc ) {    
  return {
    auth: 
      function( login, password ) {
        return RESTSrvc.getPromise({
                                    method: 'POST', 
                                    url: DemoSetting.appName + '/login', 
                                    data: {'Username' : login, 'Password' : password}
                                  });
      }
  }
};

// resolving minification problems
LoginSrvc.$inject = [ 'RESTSrvc' ];
servicesModule.factory( 'LoginSrvc', LoginSrvc );]]></CSP>
</Export>
