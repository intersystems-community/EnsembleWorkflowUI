<?xml version="1.0" encoding="UTF-8"?>
<Export generator="Cache" version="25">
<CSP name="js/services/WorklistSrvc.js" application="/workflow/"><![CDATA[
'use strict';

// Worklist service
function WorklistSrvc(RESTSrvc) {    
  return {
    // save worklist object
    save: 
      function(worklist) {
        return RESTSrvc.getPromise( {method: 'POST', url: RESTWebApp.appName + '/tasks/' + worklist._id, data: worklist} );
      },
    
    // get worklist by id 
    get: 
      function(id) {
        return RESTSrvc.getPromise( {method: 'GET', url: RESTWebApp.appName + '/tasks/' + id} );
      },
    
    // get all worklists for current user
    getAll: 
      function() {
        return RESTSrvc.getPromise( {method: 'GET', url: RESTWebApp.appName + '/tasks'} );
      }
  }
};

// resolving minification problems
WorklistSrvc.$inject = ['RESTSrvc'];
servicesModule.factory('WorklistSrvc', WorklistSrvc);
]]></CSP>
</Export>
